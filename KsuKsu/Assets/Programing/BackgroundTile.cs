using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BackgroundTile : MonoBehaviour
{

    //public GameObject[] circles;
    // Start is called before the first frame update
    void Start()
    {
        //Initialize();
    }

    private void Initialize()
    {
        //int selectedCircle = Random.Range(0, circles.Length);
        //GameObject circle = Instantiate(circles[selectedCircle], transform.position, Quaternion.identity);
        //circle.transform.SetParent(this.transform);
        //circle.name = this.gameObject.name;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
